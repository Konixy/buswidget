{
  "name": "@buswidget/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "dev:lan": "HOST=0.0.0.0 bun run --watch src/index.ts",
    "dev:local": "HOST=127.0.0.1 bun run --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src/env.test.ts src/index.test.ts",
    "test:real": "bun test src/real-api.test.ts"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "gtfs-realtime-bindings": "^1.1.1",
    "hono": "^4.8.12",
    "jszip": "^3.10.1",
    "valibot": "^0.42.1"
  },
  "devDependencies": {
    "@types/jszip": "^3.4.1",
    "bun-types": "^1.2.22"
  }
}
